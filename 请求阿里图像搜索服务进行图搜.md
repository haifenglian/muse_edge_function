## 需求-商品匹配（图搜）

对 Supabase 中的 **ai_match** 表做「以图搜图」：用每行的裁剪图调用阿里云商品图搜接口，把最高置信度的商品 ID 与分数写回该行。

### 业务规则

1. **触发条件**：仅处理 `crop_image` 非空且（建议）`standard_product_id` 为空的记录，避免重复匹配已填写的行。
2. **调用方式**：对每条记录调用阿里云 **SearchImage** 接口；请求时用该行的 `category_id` 作为 **categoryId**，只在该品类图库内检索。
3. **回写结果**：取返回结果中**置信度（Score）最高**的一条，将其 **ProductId** 写入 `ai_match.standard_product_id`，**Score** 写入 `ai_match.confidence`。
4. **crop_image**：图片 URL，需公网可访问（阿里云会拉取该 URL 做检索）。

### 字段映射（ai_match → SearchImage 入参）

| ai_match 字段   | SearchImage input | 说明 |
|-----------------|-------------------|------|
| crop_image      | picUrl            | 必填，待检索图片 URL |
| category_id     | categoryId        | 可选，整数；为空时是否传由实现决定（见下文） |
| （固定）        | instanceName      | 固定 `"muse"`，与入图实例一致 |
| （固定）        | num               | 建议 1，只取 Top1 |
| （固定）        | start             | 0 |
| （可选）        | scoreThreshold    | 最低分数阈值，如 0.48，低于则不回写 |

### 回写映射（SearchImage 出参 → ai_match）

| SearchImage 返回        | ai_match 字段          |
|-------------------------|------------------------|
| Auctions[0].ProductId   | standard_product_id     |
| Auctions[0].Score       | confidence             |

若无结果（Auctions 为空或 Success 为 false），不更新该行，或仅更新 error_message 等状态字段（若表有设计）。

### 边界与策略

- **category_id 为空**：可选策略——(a) 不传 categoryId，在全图库检索；或 (b) 跳过该行不调用。建议在文档中明确选用哪一种。
- **API 失败/超时**：重试策略（次数、间隔）、是否标记为「待重试」或写入错误信息，需在实现里约定。
- **限流**：阿里云实例为 5 次/秒，与 AddImage 一致，调用 SearchImage 时也需控制为约 **5 QPS**（如每 200ms 一条）。
- **已匹配行**：仅当 `standard_product_id IS NULL` 时再调用图搜，避免重复覆盖；若需「重新匹配」可后续加开关或单独任务。
- **一直 no match**：多半是阿里云实例里**还没有图**（图片数量为 0）或品类/分数不匹配。建议：(1) 先跑通 **consume-aliyun-sync-tasks**，用**真实 pic_url** 入图，等实例有图后再图搜；(2) 实现上已对「no match」行写 **last_search_at**，拉取时只选「未搜过或距上次搜超过 1 小时」的行，同一行不会每分钟都重试，1 小时后再试（例如图库后来入图了）。

### 实现形态建议

- **方式 A**：新增 Edge Function（如 `search-image-ai-match`），由 **pg_cron** 每分钟拉取一批「待图搜」的 ai_match 行，按 5 QPS 调 SearchImage 并回写；与现有 `consume-aliyun-sync-tasks` 类似。
- **方式 B**：离线/定时脚本（Python 等）轮询 ai_match 表，调阿里云接口并写回库。  
推荐 **方式 A**，与现有入图、同步架构统一，便于复用 HASURA_API_TOKEN 与 Vault。


## 数据表(源)
```shell
create table public.ai_match (
  id uuid not null default gen_random_uuid (),
  source_table text not null,
  source_id text not null,
  standard_product_id text null,
  confidence double precision not null,
  crop_image text null,
  create_time timestamp with time zone null default now(),
  review_status text null default 'pending'::text,
  reviewed_at timestamp with time zone null,
  category_id text null,
  image_index integer null,
  source_name text null,
  constraint ai_match_pkey primary key (id),
  constraint ai_match_confidence_check check (
    (
      (confidence >= (0)::double precision)
      and (confidence <= (1)::double precision)
    )
  ),
  constraint ai_match_review_status_check check (
    (
      review_status = any (
        array[
          'pending'::text,
          'approved'::text,
          'rejected'::text
        ]
      )
    )
  ),
  constraint ai_match_source_table_check check (
    (
      source_table = any (
        array[
          'ods_social_media'::text,
          'ods_fans'::text,
          'ods_fashion_media'::text,
          'ods_ecommerce'::text,
          'ods_brand'::text
        ]
      )
    )
  )
) TABLESPACE pg_default;

create index IF not exists idx_ai_match_source on public.ai_match using btree (source_table, source_id) TABLESPACE pg_default;

create index IF not exists idx_ai_match_confidence on public.ai_match using btree (confidence) TABLESPACE pg_default;

create index IF not exists idx_ai_match_create_time on public.ai_match using btree (create_time) TABLESPACE pg_default;

create index IF not exists idx_ai_match_product on public.ai_match using btree (standard_product_id) TABLESPACE pg_default;

create unique INDEX IF not exists idx_ai_match_unique on public.ai_match using btree (source_table, source_id, standard_product_id) TABLESPACE pg_default;

create index IF not exists idx_ai_match_review_status on public.ai_match using btree (review_status) TABLESPACE pg_default;

create index IF not exists idx_ai_match_category_id on public.ai_match using btree (category_id) TABLESPACE pg_default;
```


## 阿里云图搜接口

请求地址：https://hasura-auth-worker.data-d1a.workers.dev/

```shell
额外请求头
{
  "Authorization": "Bearer xxxxxxxx"
}
```

graphql

```shell
mutation SearchImage($input: SearchImageInput!) {
  ali { searchImage(input: $input) {
    PicInfo {
      CategoryId
      Region
    }
    Auctions {
      CategoryId
      CustomContent
      IntAttr
      PicName
      ProductId
      Score
    }
    RequestId
    Success
  }
  }}
```

variables

```json
{
  "input": {
    "instanceName": "muse",
    "picUrl": "https://cdn.shopify.com/s/files/1/0011/0382/2914/products/noah.jpg?v=1550144438",
    "num": 1,
    "start": 0,
    "scoreThreshold": "0.48",
    "categoryId": 3
  }
}
```

- **categoryId**：整数，对应品类图库；与入图时的品类一致。ai_match.category_id 若为文本需转为数字（如 `parseInt(category_id, 10)`）。
- **scoreThreshold**：低于此分数的结果可不回写或过滤。

### 返回结构（SearchImage）

- **Success**：是否成功。
- **Auctions**：数组，每个元素包含 **ProductId**（标准品 ID）、**Score**（置信度 0～1）、**PicName**、**CustomContent**、**CategoryId** 等。
- 取 **Auctions[0]** 作为最高分结果，其 **ProductId** → `standard_product_id`，**Score** → `confidence`。
- **PicInfo**：请求图中的品类等信息（可选使用）。

### 鉴权与限流

- 请求头：`Authorization: Bearer <HASURA_API_TOKEN>`，与 AddImage 使用同一 token。
- 实例最大访问频率 **5 次/秒**，实现时需按 5 QPS 限流（如每 200ms 调用一次）。

### 官方文档

https://help.aliyun.com/zh/image-search/developer-reference/api-searchbypic

---

## 部署（Edge Function + 定时任务）

- **ai_match** 表已存在；**HASURA_API_TOKEN** 与现有项目一致（与 consume-aliyun-sync-tasks 共用）。
- 部署后由 pg_cron 每分钟 POST 一次该 Edge Function，持续消化「待匹配」的 ai_match 行。

### 1. 部署 Edge Function

```bash
cd <项目根目录>
supabase functions deploy search-image-ai-match --project-ref fxllicysqmrtnpxjvldv --workdir .
```

- 无需单独配置 HASURA_API_TOKEN（与现有 Edge Function 共用项目级 Secret）。
- 可选：为该函数单独配置 **ALIYUN_GRAPHQL_URL**、**INSTANCE_NAME**、**BATCH_SIZE**、**SCORE_THRESHOLD**。

### 2. 增加 last_search_at 列（限制 no match 重试）

在 Supabase SQL Editor 中执行 `supabase/migrations/005_ai_match_last_search_at.sql`，为 ai_match 增加 `last_search_at`。no match 时会更新该字段，拉取时只选「未搜过或距上次搜超过 1 小时」的行，避免同一行被每分钟重试。

### 3. 启用定时任务

在 Supabase SQL Editor 中执行 `supabase/migrations/004_cron_search_image_ai_match.sql` 中的 `cron.schedule(...)` 部分（若 002 已执行过，pg_cron/pg_net 与 Vault anon_key 已就绪）。

### 4. 接口说明

- **GET** `https://<project>.supabase.co/functions/v1/search-image-ai-match`  
  返回当前「待匹配」数量（crop_image 非空且 standard_product_id 为空），不执行匹配。
- **POST** 同上  
  拉取一批待匹配行，按 5 QPS 调 SearchImage，将最高分结果的 ProductId/Score 回写到 standard_product_id/confidence。
- 请求头需带 `Authorization: Bearer <anon_key>` 或 Service Role Key。

---

## Mock 数据（ai_match）

以下 SQL 可在 Supabase SQL Editor 中执行，插入一组「待图搜」的 mock 行（crop_image 非空、standard_product_id 为空，便于触发 search-image-ai-match）。

```sql
INSERT INTO public.ai_match (
  source_table,
  source_id,
  standard_product_id,
  confidence,
  crop_image,
  review_status,
  category_id,
  image_index,
  source_name
) VALUES
  (
    'ods_ecommerce',
    'mock_sku_001',
    NULL,
    0,
    'https://storage.googleapis.com/material_management_system/image/processed_image_20260119_081137_9334c2ef.jpg',
    'pending',
    '3',
    0,
    'mock_product_1'
  ),
  (
    'ods_social_media',
    'mock_post_002',
    NULL,
    0,
    'https://storage.googleapis.com/material_management_system/image/processed_image_20260106_074936_748302f2.jpg',
    'pending',
    '3',
    0,
    'mock_post_2'
  ),
  (
    'ods_brand',
    'mock_brand_003',
    NULL,
    0,
    'https://storage.googleapis.com/material_management_system/image/processed_image_20260104_103802_fbf1a8cb.jpg',
    'pending',
    '1',
    0,
    'mock_brand_3'
  ),
  (
    'ods_fashion_media',
    'mock_fashion_004',
    NULL,
    0,
    'https://storage.googleapis.com/material_management_system/image/processed_image_20251231_091606_46e4dbd5.jpg',
    'pending',
    NULL,
    0,
    'mock_fashion_4'
  ),
  (
    'ods_fans',
    'mock_fans_005',
    NULL,
    0,
    'https://storage.googleapis.com/material_management_system/image/processed_image_20260109_023432_5bca445f.jpg',
    'pending',
    '3',
    1,
    'mock_fans_5'
  );
```

- **crop_image**：均为公网可访问图片 URL（Shopify / Unsplash），便于阿里云拉图检索。
- **category_id**：3、1 或 NULL（NULL 时 SearchImage 不传 categoryId，全图库检索）。
- **standard_product_id**：NULL，confidence 为 0，表示待匹配；跑完 search-image-ai-match 后会被回写。